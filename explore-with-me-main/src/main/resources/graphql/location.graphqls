# Event location
type Location {
    # Location id
    id: ID
    # Location latitude
    lat: Float
    # Location longitude
    lon: Float
}

# Input for locations
input LocationInput {
    # Location latitude
    lat: Float
    # Location longitude
    lon: Float
}

extend type Query {
    # Get location by id
    location(id: ID!): Location
}